package lshh.modules.client.seller.service;

import org.springframework.cloud.openfeign.FeignClient;

//@FeignClient(name = "payment-service", url = "${payment-service.url}")
public interface SellerServiceFeignClient {
    // todo
}
